[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
timeout = 120
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    login: marks tests that require portal login
